Import('env')

for subdir in ['usenglish', 'cmulex', 'cmu_us_awb']:
    objs = []
    objs.extend(env.SConscript('%s/SConscript' % subdir, {'env': env}))
    env.StaticLibrary('#/lib/flite_'+subdir, objs)
